# WaveCoin (WAV) - Token Platform

A full-stack Web3 application for managing decentralized assets.

## 🚀 Setup & Execution
1. Navigate to the project:
   ```bash
   cd wavcoin
   dfx start --background
   dfx deploy
npm install
npm start(use wsl + ubantu)
